{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "DROPS Environment Configuration",
    "type": "object",
    "required": ["id", "access", "paths"],
    "properties": {
        "id": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9_-]*$",
            "description": "Unique identifier for the environment"
        },
        "label": {
            "type": "string",
            "description": "Human-readable name"
        },
        "access": {
            "type": "object",
            "required": ["type"],
            "properties": {
                "type": {
                    "type": "string",
                    "enum": ["local", "ssh"],
                    "description": "Access strategy"
                },
                "host": {
                    "type": "string",
                    "description": "SSH hostname or IP"
                },
                "port": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 65535,
                    "default": 22,
                    "description": "SSH port"
                },
                "user": {
                    "type": "string",
                    "description": "SSH username"
                },
                "identity_file": {
                    "type": "string",
                    "description": "Path to SSH private key"
                },
                "exec": {
                    "type": "string",
                    "description": "Command prefix for containerised environments (e.g. ddev exec -p myproject, lando ssh -c)"
                }
            },
            "if": {
                "properties": { "type": { "const": "ssh" } }
            },
            "then": {
                "required": ["host", "user"]
            }
        },
        "paths": {
            "type": "object",
            "required": ["webroot"],
            "properties": {
                "webroot": {
                    "type": "string",
                    "description": "Absolute path to Drupal webroot"
                },
                "drush": {
                    "type": "string",
                    "description": "Path to Drush executable"
                },
                "php": {
                    "type": "string",
                    "description": "Path to PHP binary"
                },
                "temp": {
                    "type": "string",
                    "description": "Temp directory for staging files"
                },
                "private_files": {
                    "type": "string",
                    "description": "Absolute path to the Drupal private file system (file_private_path)"
                }
            }
        },
        "uri": {
            "type": "string",
            "description": "Drupal site URI for multi-site installs (e.g. site-a.example.com). When set, Drush commands include --uri and file operations target sites/<uri>/ instead of sites/default/."
        },
        "env_vars": {
            "type": "object",
            "additionalProperties": {
                "type": "string"
            },
            "description": "Extra environment variables for hook scripts"
        }
    },
    "additionalProperties": false
}
